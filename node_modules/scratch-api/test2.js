const Scratch = require('.');
//Scratch.UserSession.load(function(err, user) {
  Scratch.getProject(798796, (e, p) => console.log(e, p));
  //user.cloudSession(324133277, function(err, cloud) {
    //cloud.on('set', (name, value) => console.log(name, value));
  //});
//});
